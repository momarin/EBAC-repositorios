# Exercícios e aulas da parte de front-end

# CSS

1. Centralização do conteúdo no CSS no style do webStorage;

# JS

1. principais métodos da localStorage:

- localStorage.setItem("chave", valor);
- localStorage.getItem("chave");
- localStorage.removeItem("chave");
- localStorage.clear();

2. Tratando erros no Fetch

- fetchErros;
